{
  "name": "zdf-debug",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "package": "npm run build && cd dist && zip -1 -x \"*screens*\" -x \"*metadata*\" -x \"*/\\.*\" x \".*\" -x \"*.zip\" -r ../addon.zip ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
